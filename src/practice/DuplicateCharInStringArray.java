package practice;

import org.junit.Test;

public class DuplicateCharInStringArray {
	
	/*
	 * Given an array of strings made only from lowercase letters, return a list of
	 * all characters that show up in all strings within the list (including
	 * duplicates). For example, if a character occurs 3 times in all strings but
	 * not 4 times, you need to include that character three times in the final
	 * answer. You may return the answer in any order. Input:
	 * ["bella","label","roller"] Output: ["e","l","l"] Input:
	 * ["cool","lock","cook"] Output: ["c","o"]
	 */
	
	/*
	 * //Approach 1. Add the first string in array to a map with char, integer 
	 * 2. Iterate from 2nd to last element of the string array 
	 * 3. Convert each string to character array, declare a newmap 
	 * 4. check if the oldmap already contains the character, if it does not
	 */
	
	
	
	
	@Test
	public void eg1() {
		String[] input= {"bella","label","roller"};
	}

}
